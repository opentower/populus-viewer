# Використовуємо базовий образ Node.js
FROM node:latest

# Встановлюємо директорію робочого простору в контейнері
WORKDIR /app

# Копіюємо файли з репозиторію у контейнер
COPY . /app

# Виконуємо команди для встановлення залежностей та збірки проекту
RUN npm install
RUN npm run-script build

# Відкриваємо порт для доступу до додатку
EXPOSE 9000

# Запускаємо сервер
CMD ["npm", "run-script", "serve"]